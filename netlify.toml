# 1️⃣ Tells Netlify how to build your app, and where the files are
[build]
  command = "npx elm-app build"
  publish = "build"

# 2️⃣ Handles SPA redirects so all your pages work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200